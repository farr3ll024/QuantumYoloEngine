# core engine deps (unchanged)
pyyaml
rich
pandas
plotly
requests

# dash dashboard (replaces streamlit)
dash>=2.16.0
dash-bootstrap-components>=1.6.0

# kept in case you still want the streamlit dashboard alongside
# streamlit>=1.35.0